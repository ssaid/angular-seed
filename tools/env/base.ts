import { EnvConfig } from './env-config.interface';

const BaseConfig: EnvConfig = {
  // Sample API url
  API: 'http://app',
  DB: 'test',
  USER: 'admin',
  PASS: 'admin',
};

export = BaseConfig;

